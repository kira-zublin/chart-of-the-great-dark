{
  "name": "chart-of-the-great-dark",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check api/auth.js && node --check api/characters.js && node --check api/image.js && node --check app.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.0"
  }
}
